FROM alpine:latest

RUN mkdir -p /src
WORKDIR /src

RUN apk add --no-cache bash ca-certificates curl git jq gettext kind kubectl helm yq-go docker-cli libc6-compat

# Install Openshift CLI
ARG TARGETARCH
RUN OC_ARCH=$([ "$TARGETARCH" = "arm64" ] && echo "aarch64" || echo "x86_64") && \
    curl -fsSL "https://mirror.openshift.com/pub/openshift-v4/${OC_ARCH}/clients/ocp/stable/openshift-client-linux.tar.gz" \
      -o oc.tar.gz && \
    tar -xzf oc.tar.gz oc && \
    mv oc /usr/local/bin/ && \
    rm oc.tar.gz

RUN git config --global --add safe.directory /src

ENV CONTAINERIZED=true
